// Header Includes
#include "database.h"

void Database::populateCache() {
    // Load last 500 orders from db
}

void Database::openConnection() {
    // if (instance.db == nullptr) {
                // sqlite3_open(DB1, &instance.db);
        // }
}

void Database::closeConnection() {
    // if (db != nullptr) {
        // sqlite3_close(db);
        // db = nullptr;
    // }
}
